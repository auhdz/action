// Rename or copy this file to `Secrets.xcconfig` next to `Shared.xcconfig` and add your Supabase project values.
// In Xcode: Project > Info > Configurations > set Debug/Release to use Shared.xcconfig (already linked in the project).

SUPABASE_URL = https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY = YOUR_SUPABASE_ANON_KEY
